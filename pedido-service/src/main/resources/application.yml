spring:
  application:
    name: pedido-service

  kafka:
    bootstrap-servers: kafka:29092

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka

server:
    port: 8001